<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    <title>Nucleus — RAG embebido (Blazor)</title>
    <style>
        body { font-family: system-ui, sans-serif; max-width: 760px; margin: 2rem auto; padding: 0 1rem; color: #1a1a1a; line-height: 1.5; }
        h1 { font-size: 1.5rem; }
        h2 { font-size: 1.1rem; margin-top: 1.8rem; }
        section { border: 1px solid #e2e2e2; border-radius: 10px; padding: 1rem 1.2rem; margin-top: 1rem; }
        input, textarea { width: 100%; padding: .5rem; margin: .3rem 0; border: 1px solid #ccc; border-radius: 6px; box-sizing: border-box; font: inherit; }
        button { padding: .5rem 1rem; border: 1px solid #2563eb; background: #2563eb; color: #fff; border-radius: 6px; cursor: pointer; font: inherit; }
        button:disabled { opacity: .5; cursor: default; }
        .muted { color: #666; font-size: .9rem; }
        .hit { border-top: 1px solid #eee; padding: .5rem 0; }
        .score { font-variant-numeric: tabular-nums; color: #2563eb; font-weight: 600; margin-right: .5rem; }
    </style>
    <HeadOutlet />
</head>
<body>
    <Routes />
    <script src="_framework/blazor.web.js"></script>
</body>
</html>
